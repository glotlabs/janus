data_dir = "/var/lib/strait-server"
repos_dir = "/var/lib/strait-server/repos"

[database]
path = "/var/lib/strait-server/db/server.sqlite3"

[server]
listen = "127.0.0.1:8090"
public_base_url = "ci.example.internal"

[auth]
session_secret = "replace-me-with-a-long-random-secret"
session_ttl_days = 7
session_cookie_secure = true
login_rate_limit_per_minute = 10

[auth.bootstrap_admin]
username = "admin"
password = "change-me"

[runner_auth]
key_id = "main-strait-2026-05"
private_key_path = "/var/lib/strait-server/keys/runner-signing.key"
public_key_path = "/var/lib/strait-server/keys/runner-signing.pub"

[scheduler]
poll_interval_ms = 2000
cancel_stuck_timeout_seconds = 30
max_cancel_retries = 3
max_infra_retries = 2

[runners]
healthcheck_interval_seconds = 30
